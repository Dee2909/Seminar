{
    "name": "seminarcomp-root",
    "version": "1.0.0",
    "scripts": {
        "install:all": "npm install && cd client && npm install && cd ../server && npm install",
        "build": "cd client && npm install && npm run build && cd .. && mkdir -p server/built && cp -r client/dist/* server/built/ && cd server && npm install",
        "start": "cd server && npm start"
    }
}